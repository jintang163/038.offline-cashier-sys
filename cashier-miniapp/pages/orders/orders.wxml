<view class="orders-page">
  <view class="tabs">
    <view
      wx:for="{{tabs}}"
      wx:key="key"
      class="tab-item {{activeTab === item.key ? 'active' : ''}}"
      data-tab="{{item.key}}"
      bindtap="onTabTap"
    >
      {{item.name}}
    </view>
  </view>

  <view wx:if="{{orderList.length === 0 && !isLoading}}" class="empty">
    <image src="/images/empty/order-empty.png" mode="aspectFit"></image>
    <text>{{i18n['order.noOrders']}}</text>
    <view class="go-menu-btn" bindtap="onGoMenu">{{i18n['cart.goOrder']}}</view>
  </view>

  <scroll-view wx:else class="order-list" scroll-y enable-back-to-top>
    <view
      wx:for="{{orderList}}"
      wx:key="id"
      class="order-card"
      data-id="{{item.id}}"
      bindtap="onOrderTap"
    >
      <view class="order-header">
        <text class="order-no">{{i18n['order.orderNo']}}：{{item.orderNo}}</text>
        <text class="order-status" style="color: {{item.statusColor}}">{{item.statusText}}</text>
      </view>

      <view class="order-content">
        <image class="goods-image" src="{{item.firstGoods.image}}" mode="aspectFill"></image>
        <view class="goods-info">
          <view class="goods-name text-ellipsis">{{item.firstGoods.name}}</view>
          <view class="goods-more" wx:if="{{item.goods.length > 1}}">
            {{item.moreGoodsText}}
          </view>
          <view class="goods-count">{{item.goodsCountText}}</view>
        </view>
        <view class="order-amount">
          <text class="amount-label">{{i18n['common.total']}}</text>
          <text class="amount-value">{{item.totalAmount}}</text>
        </view>
      </view>

      <view class="order-footer">
        <text class="order-time">{{item.createTimeText}}</text>
        <view class="order-actions">
          <view
            wx:if="{{item.status === 'pending'}}"
            class="action-btn cancel"
            data-id="{{item.id}}"
            catchtap="onCancelOrder"
          >
            {{i18n['order.cancelOrder']}}
          </view>
          <view
            wx:if="{{item.status === 'pending'}}"
            class="action-btn primary"
            data-id="{{item.id}}"
            catchtap="onPayOrder"
          >
            {{i18n['order.payNow']}}
          </view>
          <view
            wx:if="{{item.status === 'completed'}}"
            class="action-btn primary"
            data-id="{{item.id}}"
            catchtap="onOrderTap"
          >
            {{i18n['order.reorder']}}
          </view>
        </view>
      </view>
    </view>
  </scroll-view>
</view>
